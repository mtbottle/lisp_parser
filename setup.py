from setuptools import setup

setup(
    name="Lisp Parser",
    version="0.0.1",
    packages=['lisp_parser'],
    install_requires=[],
    setup_requires=['pytest-runner'],
    tests_requires=['pytest']
)
